===== SECTION: REPORT =====
1. РќР°Р»РёС‡РёРµ РёСЃС…РѕРґРЅРѕРіРѕ РєРѕРґР°:
   Р—Р°СЏРІР»РµРЅРёРµ Рѕ С‚РѕРј, С‡С‚Рѕ РєРѕРґР° С„РёР»СЊС‚СЂР° РїРѕ СЃР»СЌС€Сѓ Рё РєР»Р°СЃСЃР° UiHarvest РЅРµС‚ РІ РїСЂРѕРµРєС‚Рµ, Р±С‹Р»Рѕ РѕС€РёР±РєРѕР№ РёР·-Р·Р° РЅРµРІРµСЂРЅРѕР№ РёРЅС‚РµСЂРїСЂРµС‚Р°С†РёРё СЃРѕСЃС‚Р°РІР° РїСЂРµРґС‹РґСѓС‰РёС… РёРЅРєСЂРµРјРµРЅС‚Р°Р»СЊРЅС‹С… Р±Р°РЅРґР»РѕРІ (РІ РЅРёС… РІРєР»СЋС‡Р°Р»РёСЃСЊ С‚РѕР»СЊРєРѕ РёР·РјРµРЅРµРЅРЅС‹Рµ РІ РєРѕРјРјРёС‚Р°С… С„Р°Р№Р»С‹). Р¤Р°Р№Р» Source/Patches.cs СЃРѕРґРµСЂР¶РёС‚ Рё UiHarvest, Рё РјРµС‚РѕРґ Drain(), Рё С„РёР»СЊС‚СЂ СЃР»СЌС€РµР№.
2. РЎСѓРґСЊР±Р° РѕР±СЂР°Р·С†РѕРІ:
   - "Death-in threshold: 8 hours" Рё "Assist bonus (per extra doctor, base): +80% speed" СѓСЃРїРµС€РЅРѕ РїСЂРѕС…РѕРґСЏС‚ РІСЃСЋ С†РµРїРѕС‡РєСѓ Рё РѕС‚РїСЂР°РІР»СЏСЋС‚СЃСЏ РІ РѕС‡РµСЂРµРґСЊ РїРµСЂРµРІРѕРґР°, С‚Р°Рє РєР°Рє РЅРµ СЃРѕРґРµСЂР¶Р°С‚ СЃР»СЌС€РµР№ Рё РЅРµ СЂР°СЃРїРѕР·РЅР°СЋС‚СЃСЏ РєР°Рє РёРјРµСЋС‰РёРµ С‡РёСЃР»РѕРІРѕР№ С…РІРѕСЃС‚ (РёР·-Р·Р° Р±СѓРєРІРµРЅРЅС‹С… РѕРєРѕРЅС‡Р°РЅРёР№ "hours" Рё "speed").
   - "Bleed rate threshold: 100%/day" РѕС‚Р±СЂР°СЃС‹РІР°РµС‚СЃСЏ РІ UiHarvest.Drain() РёР·-Р·Р° РЅР°Р»РёС‡РёСЏ СЃР»СЌС€Р° '/'.
   - "Decay per additional doctor: Г—0.75" РќР• РѕС‚Р±СЂР°СЃС‹РІР°РµС‚СЃСЏ СЃР»СЌС€РµРј, РЅРѕ РёР·-Р·Р° РєСЂРёС‚РёС‡РµСЃРєРѕР№ РѕС€РёР±РєРё РєРѕРґРёСЂРѕРІРєРё РІ Patches.cs (РіРґРµ СЃРёРјРІРѕР» 'Г—' Р·Р°РјРµРЅРёР»СЃСЏ РЅР° '?') С…РІРѕСЃС‚ РЅРµ СЂР°СЃРїРѕР·РЅР°РµС‚СЃСЏ РєР°Рє С‡РёСЃР»РѕРІРѕР№ РІ UiHarvest.Note. Р’СЃСЏ СЃС‚СЂРѕРєР° РѕС‚РїСЂР°РІР»СЏРµС‚СЃСЏ РІ РѕС‡РµСЂРµРґСЊ РєР°Рє РµРґРёРЅРѕРµ С†РµР»РѕРµ, РЅРѕ РЅРµ РїРµСЂРµРІРѕРґРёС‚СЃСЏ РЅР° СЌРєСЂР°РЅРµ РёР·-Р·Р° РѕС‚СЃСѓС‚СЃС‚РІРёСЏ С‚РѕС‡РЅРѕРіРѕ СЃРѕРІРїР°РґРµРЅРёСЏ РІ РєСЌС€Рµ.
3. РўРµРіРё:
   РўРµРіРё РІСЂРѕРґРµ <b> РїРµСЂРµРґ РїРѕРёСЃРєРѕРј РІ РєСЌС€Рµ Рё РїРµСЂРµРґ РїРѕСЃС‚Р°РЅРѕРІРєРѕР№ РІ РѕС‡РµСЂРµРґСЊ РќР• СЃСЂРµР·Р°СЋС‚СЃСЏ. РњРµС‚РѕРґ TryGetFlat РІС‹РїРѕР»РЅСЏРµС‚ С‚РѕР»СЊРєРѕ СЃС‚СЂРѕРіРѕРµ СЃРѕРІРїР°РґРµРЅРёРµ РєР»СЋС‡Р°.

===== SECTION: TASK1_SLASH =====
Р’С‹РІРѕРґ РєРѕРјР°РЅРґС‹ Select-String:
Path                                                                                LineNumber Line
----                                                                                ---------- ----
D:\Ayder_dontdelete\rimka_translate\GlobalAutoTranslator\Source\Patches.cs                 162 				if (s.Contains("/") || s.Contains("\\") ||

РљРѕРЅС‚РµРєСЃС‚ РІРѕРєСЂСѓРі СЃС‚СЂРѕРєРё 162 РІ Source/Patches.cs:
147: 			}
148: 
149: 			string s;
150: 			int examined = 0;
151: 			while (examined < maxPerCall && pending.TryDequeue(out s))
152: 			{
153: 				examined++;
154: 
155: 				if (!PlaceholderGuard.ShouldTranslate(s))
156: 				{
157: 					filteredCount++;
158: 					continue;
159: 				}
160: 
161: 				// Р¤РёР»СЊС‚СЂ РїСѓС‚РµР№, С„Р°Р№Р»РѕРІ Рё СЂР°СЃС€РёСЂРµРЅРёР№
162: 				if (s.Contains("/") || s.Contains("\\") ||
163: 					s.EndsWith(".png", StringComparison.OrdinalIgnoreCase) ||
164: 					s.EndsWith(".xml", StringComparison.OrdinalIgnoreCase) ||
165: 					s.EndsWith(".dll", StringComparison.OrdinalIgnoreCase))
166: 				{
167: 					filteredCount++;
168: 					continue;
169: 				}
170: 
171: 				// РџСЂРѕРІРµСЂРєР° С†РёС„СЂ
172: 				int digits = 0;
173: 				bool hasLetter = false;
174: 				for (int i = 0; i < s.Length; i++)
175: 				{
176: 					char c = s[i];
177: 					if (char.IsDigit(c)) digits++;
178: 					if (char.IsLetter(c)) hasLetter = true;
179: 				}

===== SECTION: CHAIN =====
РџРѕР»РЅР°СЏ С†РµРїРѕС‡РєР° РѕС‚СЃРµРІР° РѕС‚ Widgets.Label РґРѕ TranslateWorker.Enqueue:

1. Patches.cs:537 (Patch_Widgets_Label_RectString.Prefix)
   РЈСЃР»РѕРІРёРµ: if (s == null || !s.translateWidgets) return;
   Р РµР·СѓР»СЊС‚Р°С‚: РІС‹С…РѕРґ РёР· РїР°С‚С‡Р°, РїРµСЂРµРІРѕРґ РёРЅС‚РµСЂС„РµР№СЃР° РѕС‚РєР»СЋС‡РµРЅ.

2. Patches.cs:539 (Patch_Widgets_Label_RectString.Prefix)
   РЈСЃР»РѕРІРёРµ: if (string.IsNullOrEmpty(label) || label.Length > 300) return;
   Р РµР·СѓР»СЊС‚Р°С‚: РІС‹С…РѕРґ РёР· РїР°С‚С‡Р° (РёРіРЅРѕСЂРёСЂСѓСЋС‚СЃСЏ СЃР»РёС€РєРѕРј РґР»РёРЅРЅС‹Рµ РёР»Рё РїСѓСЃС‚С‹Рµ СЃС‚СЂРѕРєРё).

3. Patches.cs:542 (Patch_Widgets_Label_RectString.Prefix)
   РЈСЃР»РѕРІРёРµ: if (TranslationCache.TryGetFlat(label, out cached)) label = cached; else UiHarvest.Note(label);
   Р РµР·СѓР»СЊС‚Р°С‚: РµСЃР»Рё РїРµСЂРµРІРѕРґ РЅР°Р№РґРµРЅ РІ РїР»РѕСЃРєРѕРј РєСЌС€Рµ, РїРѕРґРјРµРЅСЏРµС‚СЃСЏ. Р•СЃР»Рё РЅРµС‚ вЂ” СѓС…РѕРґРёС‚ РЅР° СЃР±РѕСЂРєСѓ РІ UiHarvest.Note.

4. Patches.cs:38-40 (UiHarvest.Note)
   РЈСЃР»РѕРІРёРµ: if (string.IsNullOrEmpty(s)) return; int len = s.Length; if (len < 2 || len > 600) return;
   Р РµР·СѓР»СЊС‚Р°С‚: РѕС‚СЃРµРІ РїРѕ РґР»РёРЅРµ.

5. Patches.cs:49 (UiHarvest.Note)
   РЈСЃР»РѕРІРёРµ: if (newThisFrame >= 4) return;
   Р РµР·СѓР»СЊС‚Р°С‚: Р»РёРјРёС‚ РЅР° 4 РЅРѕРІС‹Рµ СЃС‚СЂРѕРєРё Р·Р° РѕРґРёРЅ С„СЂРµР№Рј. РџСЂРµРґРѕС‚РІСЂР°С‰Р°РµС‚ Р»Р°РІРёРЅРѕРѕР±СЂР°Р·РЅРѕРµ Р·Р°СЃРѕСЂРµРЅРёРµ РѕС‡РµСЂРµРґРё.

6. Patches.cs:53-54 (UiHarvest.Note)
   РЈСЃР»РѕРІРёРµ: if (seen.Count >= MaxSeenCapacity) return; if (seen.Contains(s)) return;
   Р РµР·СѓР»СЊС‚Р°С‚: РІС‹С…РѕРґ, РµСЃР»Рё СЃС‚СЂРѕРєР° СѓР¶Рµ РѕР±СЂР°Р±Р°С‚С‹РІР°Р»Р°СЃСЊ (РґРµРґСѓРїР»РёРєР°С†РёСЏ) РёР»Рё РґРѕСЃС‚РёРіРЅСѓС‚ Р»РёРјРёС‚ seen (100000).

7. Patches.cs:57 (UiHarvest.Note)
   РЈСЃР»РѕРІРёРµ: if (!PlaceholderGuard.NeedsTranslation(s)) { ... return; }
   Р РµР·СѓР»СЊС‚Р°С‚: РїСЂРѕРІРµСЂСЏРµС‚, РЅСѓР¶РµРЅ Р»Рё РїРµСЂРµРІРѕРґ (РЅР°РїСЂРёРјРµСЂ, РЅР°Р»РёС‡РёРµ РєРёСЂРёР»Р»РёС†С‹/Р»Р°С‚РёРЅРёС†С‹). Р•СЃР»Рё РЅРµ РЅСѓР¶РµРЅ, СЃС‚СЂРѕРєР° Р·Р°РЅРѕСЃРёС‚СЃСЏ РІ seen Рё РјРµС‚РѕРґ Р·Р°РІРµСЂС€Р°РµС‚СЃСЏ.

8. Patches.cs:70-83 (UiHarvest.Note вЂ” РІС‹РґРµР»РµРЅРёРµ С‡РёСЃР»РѕРІРѕРіРѕ С…РІРѕСЃС‚Р°)
   РЈСЃР»РѕРІРёРµ: int colonIdx = s.LastIndexOf(':'); ... Рё РїСЂРѕРІРµСЂРєР° СЃРёРјРІРѕР»РѕРІ С…РІРѕСЃС‚Р° (tail).
   Р РµР·СѓР»СЊС‚Р°С‚: РµСЃР»Рё С…РІРѕСЃС‚ РїРѕР»РЅРѕСЃС‚СЊСЋ С‡РёСЃР»РѕРІРѕР№ (С†РёС„СЂС‹, РїСЂРѕР±РµР»С‹, %, С‚РѕС‡РєРё, Р·Р°РїСЏС‚С‹Рµ, Р·РЅР°РєРё, СЃР»СЌС€, x, Г—), С‚Рѕ РІ РѕС‡РµСЂРµРґСЊ pending РєР»Р°РґРµС‚СЃСЏ С‚РѕР»СЊРєРѕ head, Р° РѕСЂРёРіРёРЅР°Р»СЊРЅР°СЏ СЃС‚СЂРѕРєР° СЃ С…РІРѕСЃС‚РѕРј Р·Р°РЅРѕСЃРёС‚СЃСЏ РІ seen.

9. Patches.cs:128-132 (UiHarvest.Note вЂ” С„РёРЅР°Р»СЊРЅР°СЏ РїРѕСЃС‚Р°РЅРѕРІРєР° РІ pending)
   РЈСЃР»РѕРІРёРµ: if (seen.Count < MaxSeenCapacity && !seen.Contains(s))
   Р РµР·СѓР»СЊС‚Р°С‚: РґРѕР±Р°РІР»РµРЅРёРµ РІ seen, РІ РѕС‡РµСЂРµРґСЊ pending Рё РёРЅРєСЂРµРјРµРЅС‚ newThisFrame.

10. Patches.cs:139 (UiHarvest.Drain)
    РЈСЃР»РѕРІРёРµ: if (enqueuedThisSession >= MaxEnqueuedPerSession) return;
    Р РµР·СѓР»СЊС‚Р°С‚: Р¶РµСЃС‚РєРёР№ Р»РёРјРёС‚ СЃРµСЃСЃРёРё (100000). Р•СЃР»Рё РґРѕСЃС‚РёРіРЅСѓС‚ вЂ” СЃР±РѕСЂ РЅРѕРІС‹С… СЃС‚СЂРѕРє РѕСЃС‚Р°РЅР°РІР»РёРІР°РµС‚СЃСЏ.

11. Patches.cs:155 (UiHarvest.Drain)
    РЈСЃР»РѕРІРёРµ: if (!PlaceholderGuard.ShouldTranslate(s)) { filteredCount++; continue; }
    Р РµР·СѓР»СЊС‚Р°С‚: РїСЂРѕРІРµСЂРєР° С„РёР»СЊС‚СЂР° РёСЃРєР»СЋС‡РµРЅРёР№ (ShouldTranslate).

12. Patches.cs:162 (UiHarvest.Drain вЂ” СЃР»СЌС€-С„РёР»СЊС‚СЂ)
    РЈСЃР»РѕРІРёРµ: if (s.Contains("/") || s.Contains("\\") || s.EndsWith(".png"...) || s.EndsWith(".xml"...) || s.EndsWith(".dll"...))
    Р РµР·СѓР»СЊС‚Р°С‚: РѕС‚СЃРµРІ РїСѓС‚РµР№ С„Р°Р№Р»РѕРІ Рё СЂР°СЃС€РёСЂРµРЅРёР№.

13. Patches.cs:181-183 (UiHarvest.Drain вЂ” С„РёР»СЊС‚СЂ С†РёС„СЂ)
    РЈСЃР»РѕРІРёРµ:
      if (!hasLetter) { filteredCount++; continue; }
      if (digits > 0 && s.Length < 25) { filteredCount++; continue; }
      if (digits * 4 > s.Length) { filteredCount++; continue; }
    Р РµР·СѓР»СЊС‚Р°С‚: РѕС‚СЃРµРІ СЃС‚СЂРѕРє Р±РµР· Р±СѓРєРІ, РєРѕСЂРѕС‚РєРёС… СЃС‚СЂРѕРє СЃ С†РёС„СЂР°РјРё (< 25 СЃРёРјРІРѕР»РѕРІ) Рё СЃС‚СЂРѕРє, СЃРѕСЃС‚РѕСЏС‰РёС… РїСЂРµРёРјСѓС‰РµСЃС‚РІРµРЅРЅРѕ РёР· С†РёС„СЂ (> 25%).

14. Patches.cs:186-191 (UiHarvest.Drain вЂ” CamelCase)
    РЈСЃР»РѕРІРёРµ: if (!s.Contains(" ")) { if (IsCamelCaseOrCodeIdentifier(s)) { filteredCount++; continue; } }
    Р РµР·СѓР»СЊС‚Р°С‚: РѕС‚СЃРµРІ РёРґРµРЅС‚РёС„РёРєР°С‚РѕСЂРѕРІ РєРѕРґР°.

15. TranslateWorker.cs:214 (TranslateWorker.Enqueue)
    РЈСЃР»РѕРІРёРµ: if (string.IsNullOrEmpty(source) || source.Length > 4000) return;
    Р РµР·СѓР»СЊС‚Р°С‚: Р·Р°С‰РёС‚Р° РїРѕ РґР»РёРЅРµ.

16. TranslateWorker.cs:217 (TranslateWorker.Enqueue)
    РЈСЃР»РѕРІРёРµ: if (quarantine.ContainsKey(key) || TranslationCache.IsPermanentFailed(key)) return;
    Р РµР·СѓР»СЊС‚Р°С‚: РѕС‚СЃРµРІ Р·Р°Р±Р»РѕРєРёСЂРѕРІР°РЅРЅС‹С… (failed.tsv) Рё РІСЂРµРјРµРЅРЅРѕ РѕС‚Р±СЂРѕС€РµРЅРЅС‹С… СЃС‚СЂРѕРє.

17. TranslateWorker.cs:234 (TranslateWorker.Enqueue)
    РЈСЃР»РѕРІРёРµ: if (!PlaceholderGuard.NeedsTranslation(source)) return;
    Р РµР·СѓР»СЊС‚Р°С‚: РґСѓР±Р»РёСЂСѓСЋС‰Р°СЏ РїСЂРѕРІРµСЂРєР° РЅРµРѕР±С…РѕРґРёРјРѕСЃС‚Рё РїРµСЂРµРІРѕРґР°.

18. TranslateWorker.cs:236 (TranslateWorker.Enqueue)
    РЈСЃР»РѕРІРёРµ: if (!inFlight.TryAdd(key, 1)) return;
    Р РµР·СѓР»СЊС‚Р°С‚: РґРµРґСѓРїР»РёРєР°С†РёСЏ РЅР° СѓСЂРѕРІРЅРµ РѕС‚РїСЂР°РІРєРё РІ РѕС‡РµСЂРµРґСЊ РїРµСЂРµРІРѕРґР°.

===== SECTION: FOUR_SAMPLES =====
РџСЂРѕС…РѕР¶РґРµРЅРёРµ РѕР±СЂР°Р·С†РѕРІ РїРѕ С†РµРїРѕС‡РєРµ:

1. "Death-in threshold: 8 hours"
   - UiHarvest.Note (РЎС‚СЂРѕРєР° 70): colonIdx = 19, tail = " 8 hours". РџСЂРѕРІРµСЂРєР° СЃРёРјРІРѕР»РѕРІ tail РЅР°С‚С‹РєР°РµС‚СЃСЏ РЅР° Р±СѓРєРІС‹ 'h', 'o', 'u', 'r', 's', РєРѕС‚РѕСЂС‹Рµ РЅРµ РІС…РѕРґСЏС‚ РІ СЂР°Р·СЂРµС€РµРЅРЅС‹Р№ СЃРїРёСЃРѕРє. isTailNumeric = false. РЎС‚СЂРѕРєР° РЅРµ СЂР°Р·РґРµР»СЏРµС‚СЃСЏ, РІ pending СѓС…РѕРґРёС‚ С†РµР»РёРєРѕРј: "Death-in threshold: 8 hours".
   - UiHarvest.Drain (РЎС‚СЂРѕРєР° 162): РЎР»СЌС€РµР№ РЅРµС‚, С„РёР»СЊС‚СЂ СЃР»СЌС€РµР№ РїСЂРѕРїСѓСЃРєР°РµС‚СЃСЏ.
   - UiHarvest.Drain (РЎС‚СЂРѕРєР° 182): Р”Р»РёРЅР° 28 (>= 25), digits = 1. РЈСЃР»РѕРІРёРµ digits > 0 && s.Length < 25 Р»РѕР¶РЅРѕ. Р¤РёР»СЊС‚СЂ С†РёС„СЂ РїСЂРѕРїСѓСЃРєР°РµС‚СЃСЏ.
   - РС‚РѕРі: РЈСЃРїРµС€РЅРѕ РїРµСЂРµС…РѕРґРёС‚ РІ TranslateWorker.Enqueue Рё РїРµСЂРµРІРѕРґРёС‚СЃСЏ.

2. "Assist bonus (per extra doctor, base): +80% speed"
   - UiHarvest.Note (РЎС‚СЂРѕРєР° 70): colonIdx = 38, tail = " +80% speed". РќР°Р»РёС‡РёРµ Р±СѓРєРІ 's', 'p', 'e', 'e', 'd' РґРµР»Р°РµС‚ isTailNumeric = false. Р’ pending СѓС…РѕРґРёС‚ С†РµР»РёРєРѕРј.
   - UiHarvest.Drain (РЎС‚СЂРѕРєР° 162): РЎР»СЌС€РµР№ РЅРµС‚.
   - UiHarvest.Drain (РЎС‚СЂРѕРєР° 182): Р”Р»РёРЅР° 51 (>= 25), digits = 2. РџСЂРѕРїСѓСЃРєР°РµС‚СЃСЏ.
   - РС‚РѕРі: РЈСЃРїРµС€РЅРѕ РїРµСЂРµС…РѕРґРёС‚ РІ TranslateWorker.Enqueue Рё РїРµСЂРµРІРѕРґРёС‚СЃСЏ.

3. "Bleed rate threshold: 100%/day"
   - UiHarvest.Note (РЎС‚СЂРѕРєР° 70): colonIdx = 21, tail = " 100%/day". РР·-Р·Р° Р±СѓРєРІ 'd', 'a', 'y' tail РЅРµ С‡РёСЃР»РѕРІРѕР№ (isTailNumeric = false). Р’ pending СѓС…РѕРґРёС‚ РІСЃСЏ СЃС‚СЂРѕРєР°.
   - UiHarvest.Drain (РЎС‚СЂРѕРєР° 162): РЎС‚СЂРѕРєР° СЃРѕРґРµСЂР¶РёС‚ '/'. РЎСЂР°Р±Р°С‚С‹РІР°РµС‚ С„РёР»СЊС‚СЂ s.Contains("/").
   - РС‚РѕРі: РЎС‚СЂРѕРєР° РѕС‚СЃРµРєР°РµС‚СЃСЏ РЅР° СЃС‚СЂРѕРєРµ 168 РІ Patches.cs Рё РЅРµ СЃС‚Р°РІРёС‚СЃСЏ РІ РѕС‡РµСЂРµРґСЊ.

4. "Decay per additional doctor: Г—0.75"
   - UiHarvest.Note (РЎС‚СЂРѕРєР° 70): colonIdx = 27, tail = " Г—0.75".
     Р’РќРРњРђРќРР•! Р’ РєРѕРґРµ Patches.cs:78 РїСЂРѕРІРµСЂРєР° tail СЃРѕРґРµСЂР¶РёС‚ РѕС€РёР±РєСѓ: СЃРёРјРІРѕР» 'Г—' (СѓРјРЅРѕР¶РµРЅРёРµ) Р·Р°РїРёСЃР°РЅ РєР°Рє '?' (Р·РЅР°Рє РІРѕРїСЂРѕСЃР°).
     РР·-Р·Р° СЌС‚РѕРіРѕ СЃРёРјРІРѕР» 'Г—' (U+00D7) РЅРµ СЂР°СЃРїРѕР·РЅР°РµС‚СЃСЏ РєР°Рє СЂР°Р·СЂРµС€РµРЅРЅС‹Р№. isTailNumeric СЃС‚Р°РЅРѕРІРёС‚СЃСЏ false.
     РЎС‚СЂРѕРєР° РЅРµ СЂР°Р·РґРµР»СЏРµС‚СЃСЏ Рё СѓС…РѕРґРёС‚ РІ pending С†РµР»РёРєРѕРј.
   - UiHarvest.Drain (РЎС‚СЂРѕРєР° 162): РЎР»СЌС€РµР№ РЅРµС‚.
   - UiHarvest.Drain (РЎС‚СЂРѕРєР° 182): Р”Р»РёРЅР° 35 (>= 25), digits = 3. РџСЂРѕРїСѓСЃРєР°РµС‚СЃСЏ.
   - РС‚РѕРі: РЎС‚СЂРѕРєР° "Decay per additional doctor: Г—0.75" СѓС…РѕРґРёС‚ РІ РѕС‡РµСЂРµРґСЊ РїРµСЂРµРІРѕРґР° С†РµР»РёРєРѕРј. РќРѕ С‚Р°Рє РєР°Рє РІ РєСЌС€Рµ TryGetFlat РёС‰РµС‚ РїРѕ head "Decay per additional doctor" (РєРѕС‚РѕСЂС‹Р№ РїРµСЂРµРІРµР»СЃСЏ СЂР°РЅРµРµ РѕС‚РґРµР»СЊРЅРѕ), Р° РЅР° СЌРєСЂР°РЅРµ СЂРёСЃСѓРµС‚СЃСЏ РїРѕР»РЅР°СЏ СЃС‚СЂРѕРєР° СЃ "Г—0.75", С‚Рѕ С‚РѕС‡РЅРѕРіРѕ СЃРѕРІРїР°РґРµРЅРёСЏ РЅРµ РїСЂРѕРёСЃС…РѕРґРёС‚ Рё РїРµСЂРµРІРѕРґ РЅРµ РїСЂРёРјРµРЅСЏРµС‚СЃСЏ.

===== SECTION: TAGS =====
Р’ РєРѕРґРµ РЅРµС‚ РѕС‡РёСЃС‚РєРё РёР»Рё СЃСЂРµР·Р°РЅРёСЏ HTML-С‚РµРіРѕРІ РІРёРґР° <b> РїРµСЂРµРґ РїРѕРёСЃРєРѕРј РІ РєСЌС€Рµ Рё РїРµСЂРµРґ РїРѕСЃС‚Р°РЅРѕРІРєРѕР№ РІ РѕС‡РµСЂРµРґСЊ.
РџРѕСЃРєРѕР»СЊРєСѓ LevelUp.Profile::DoList РїРµСЂРµРґР°РµС‚ РІ Widgets.Label СЃС‚СЂРѕРєСѓ СЃ С‚РµРіР°РјРё ("<b>Level up actions</b>"), Р° РІ РєСЌС€Рµ (Р±Р»Р°РіРѕРґР°СЂСЏ СЂР°РЅРЅРµР№ РёРЅРёС†РёР°Р»РёР·Р°С†РёРё .cctor Рё РїРµСЂРµС…РІР°С‚Сѓ РІ TryGetTextFromKey) С…СЂР°РЅРёС‚СЃСЏ С‡РёСЃС‚Р°СЏ СЃС‚СЂРѕРєР° "Level up actions", С‚Рѕ TryGetFlat("<b>Level up actions</b>", out cached) РІРѕР·РІСЂР°С‰Р°РµС‚ false. РЎС‚СЂРѕРєР° РЅРµ РїРµСЂРµРІРѕРґРёС‚СЃСЏ РЅР° СЌРєСЂР°РЅРµ.

===== SECTION: SESSION_LIMIT =====
- РћР±СЉСЏРІР»РµРЅРёРµ Р»РёРјРёС‚Р°:
  Patches.cs:28: public const int MaxEnqueuedPerSession = 100000;
- Р›РёРјРёС‚ РІ РїРѕСЃР»РµРґРЅРµР№ СЃРµСЃСЃРёРё:
  РџРѕ Р»РѕРіР°Рј РїРѕСЃР»РµРґРЅРµРµ Р·РЅР°С‡РµРЅРёРµ: UI enqueued/limit: 1086/100000 (LimitReached=False, Seen=14489)
  РћС‚РІРµС‚: Р›РёРјРёС‚ РЅРµ СѓРїРёСЂР°Р»СЃСЏ, LimitReached = False.

===== SECTION: TRACES =====
РЎС‹СЂРѕР№ РІС‹РІРѕРґ РїРѕРёСЃРєР° СЃС‚СЂРѕРє РІ С„Р°Р№Р»Р°С… РєСЌС€Р°, failed.tsv Рё Player.log:

1. TSV Cache:
Found 'Decay per additional doctor' in cache shard 39.tsv: 39a5a4e4c7337382016ad401cfda6f15	Decay per additional doctor	СЌС„С„РµРєС‚РёРІРЅРѕСЃС‚СЊ РґРѕРї. РІСЂР°С‡Р°
Found 'Type of wounds to multi-tend' in cache shard 76.tsv: 76d550a6d4bcaec1f8c3a8b68fbd63b8	Type of wounds to multi-tend	С‚РёРїС‹ СЂР°РЅ РґР»СЏ Р»РµС‡РµРЅРёСЏ РЅРµСЃРєРѕР»СЊРєРёРјРё РІСЂР°С‡Р°РјРё
Found 'Level down actions' in cache shard 81.tsv: 81cb0437dd858c32bc8ad80b36afcd56	Level down actions	РґРµР№СЃС‚РІРёСЏ РїСЂРё РїРѕРЅРёР¶РµРЅРёРё СѓСЂРѕРІРЅСЏ
Found 'Level up actions' in cache shard 9a.tsv: 9a9e2e29404b69243d67ee4fbfdbe2fd	Level up actions	РґРµР№СЃС‚РІРёСЏ РїСЂРё РїРѕРІС‹С€РµРЅРёРё СѓСЂРѕРІРЅСЏ
Found 'Efficiency scaling' in cache shard d2.tsv: d2f5d3dbcf65830bb13ef7a36f159a55	СЌС„С„РµРєС‚РёРІРЅРѕСЃС‚СЊ Р»РµС‡РµРЅРёСЏ РЅРµСЃРєРѕР»СЊРєРёРјРё РІСЂР°С‡Р°РјРё
Found 'Efficiency scaling' in cache shard ea.tsv: ea7b19323e03f8856fd38324f29cc134	Use efficiency scaling (diminishing returns)	РСЃРїРѕР»СЊР·РѕРІР°С‚СЊ РјР°СЃС€С‚Р°Р±РёСЂРѕРІР°РЅРёРµ СЌС„С„РµРєС‚РёРІРЅРѕСЃС‚Рё (СѓР±С‹РІР°СЋС‰Р°СЏ РѕС‚РґР°С‡Р°)
Found 'Preview (per worker' in cache shard fd.tsv: fd2efa1f26a69405964605362fcc991b	Preview (per worker)	РџСЂРµРґРїСЂРѕСЃРјРѕС‚СЂ (РЅР° РѕРґРЅРѕРіРѕ СЂР°Р±РѕС‡РµРіРѕ)

2. failed.tsv:
(РќРё РѕРґРЅР° РёР· СЃС‚СЂРѕРє РЅРµ РЅР°Р№РґРµРЅР° РІ failed.tsv)

3. Player.log:
(Р’ Player.log РЅРµС‚ СѓРїРѕРјРёРЅР°РЅРёР№ РґР°РЅРЅС‹С… СЃС‚СЂРѕРє, СЃРµСЃСЃРёСЏ РЅР°СЃС‚СЂРѕРµРє РјРѕРґРѕРІ РЅРµ РѕС‚РєСЂС‹РІР°Р»Р°СЃСЊ)


===== SECTION: FILES =====

=== Source/Patches.cs ===
using System;
using System.Collections.Concurrent;
using System.Collections.Generic;
using System.Reflection;
using HarmonyLib;
using Verse;
using RimWorld;
using RimWorld.Planet;

namespace GlobalAutoTranslator
{
	/// <summary>
	/// Р‘РµР·РѕРїР°СЃРЅС‹Р№ СЃР±РѕСЂС‰РёРє UI-СЃС‚СЂРѕРє РѕС‚СЂРёСЃРѕРІРєРё (РЎР»РѕР№ 3 V2).
	/// Р’С‹РїРѕР»РЅСЏРµС‚ Р±С‹СЃС‚СЂСѓСЋ С„РёР»СЊС‚СЂР°С†РёСЋ Р±РµР· Р°Р»Р»РѕРєР°С†РёР№ РІРѕ РІСЂРµРјСЏ OnGUI,
	/// Р° С‚СЏР¶РµР»С‹Р№ СЂР°Р·Р±РѕСЂ Рё РїРѕСЃС‚Р°РЅРѕРІРєСѓ РІ РѕС‡РµСЂРµРґСЊ РґРµР»Р°РµС‚ РІ Drain() РіР»Р°РІРЅРѕРіРѕ РїРѕС‚РѕРєР°.
	/// </summary>
	public static class UiHarvest
	{
		private static readonly HashSet<string> seen = new HashSet<string>(StringComparer.Ordinal);
		private static readonly ConcurrentQueue<string> pending = new ConcurrentQueue<string>();
		private static int newThisFrame;
		private static int lastFrameCount = -1;

		private static int enqueuedThisSession;
		private static int filteredCount;
		private static bool limitReachedLogged;

		private const int MaxEnqueuedPerSession = 100000;
		private const int MaxSeenCapacity = 100000;

		public static int EnqueuedThisSession => enqueuedThisSession;
		public static int SessionLimit => MaxEnqueuedPerSession;
		public static bool LimitReached => enqueuedThisSession >= MaxEnqueuedPerSession;
		public static int SeenCount { get { lock (seen) { return seen.Count; } } }

		public static void Note(string s)
		{
			if (string.IsNullOrEmpty(s)) return;
			int len = s.Length;
			if (len < 2 || len > 600) return;

			int currentFrame = UnityEngine.Time.frameCount;
			if (currentFrame != lastFrameCount)
			{
				lastFrameCount = currentFrame;
				newThisFrame = 0;
			}

			if (newThisFrame >= 4) return;

			lock (seen)
			{
				if (seen.Count >= MaxSeenCapacity) return;
				if (seen.Contains(s)) return;
			}

			if (!PlaceholderGuard.NeedsTranslation(s))
			{
				lock (seen)
				{
					if (seen.Count < MaxSeenCapacity && !seen.Contains(s))
					{
						seen.Add(s);
					}
				}
				return;
			}

			// Р•СЃР»Рё Сѓ СЃС‚СЂРѕРєРё РµСЃС‚СЊ С‡РёСЃР»РѕРІРѕР№/СЋРЅРёС‚РѕРІС‹Р№ С…РІРѕСЃС‚ РїРѕСЃР»Рµ РґРІРѕРµС‚РѕС‡РёСЏ ("Global Animation Speed: 100%"), РІ РѕС‡РµСЂРµРґСЊ РєР»Р°РґС‘Рј С‚РѕР»СЊРєРѕ РіРѕР»РѕРІСѓ ("Global Animation Speed")
			int colonIdx = s.LastIndexOf(':');
			if (colonIdx > 0 && colonIdx < s.Length - 1)
			{
				string tail = s.Substring(colonIdx + 1);
				bool isTailNumeric = true;
				for (int i = 0; i < tail.Length; i++)
				{
					char c = tail[i];
					if (!char.IsDigit(c) && c != ' ' && c != '%' && c != '.' && c != ',' && c != '-' && c != '+' && c != '/' && c != 'x' && c != 'Г—')
					{
						isTailNumeric = false;
						break;
					}
				}
				if (isTailNumeric)
				{
					string head = s.Substring(0, colonIdx).TrimEnd();
					if (head.Length >= 2)
					{
						lock (seen)
						{
							if (seen.Count < MaxSeenCapacity && !seen.Contains(s))
								seen.Add(s); // РґРѕР±Р°РІР»СЏРµРј РѕСЂРёРіРёРЅР°Р»СЊРЅСѓСЋ СЃС‚СЂРѕРєСѓ С‡С‚РѕР±С‹ РЅРµ С‚СЂРёРіРіРµСЂРёС‚СЊСЃСЏ
							if (seen.Count < MaxSeenCapacity && !seen.Contains(head))
							{
								seen.Add(head);
								pending.Enqueue(head);
								newThisFrame++;
							}
						}
						return;
					}
				}
			}

			// Р•СЃР»Рё СЃС‚СЂРѕРєР° СЃ РґРІРѕРµС‚РѕС‡РёРµРј ("Label:"), СЃС‚Р°РІРёРј РІ РѕС‡РµСЂРµРґСЊ С‚РѕР»СЊРєРѕ РѕС‡РёС‰РµРЅРЅСѓСЋ РІРµСЂСЃРёСЋ ("Label"), Р° РѕСЂРёРіРёРЅР°Р»СЊРЅСѓСЋ РґРѕР±Р°РІР»СЏРµРј РІ seen
			if (s.EndsWith(":"))
			{
				string trimmed = s.Substring(0, s.Length - 1).TrimEnd();
				if (trimmed.Length >= 2)
				{
					lock (seen)
					{
						if (seen.Count < MaxSeenCapacity && !seen.Contains(s))
							seen.Add(s); // РґРѕР±Р°РІР»СЏРµРј РѕСЂРёРіРёРЅР°Р»СЊРЅСѓСЋ СЃС‚СЂРѕРєСѓ С‡С‚РѕР±С‹ РЅРµ С‚СЂРёРіРіРµСЂРёС‚СЊСЃСЏ
						if (seen.Count < MaxSeenCapacity && !seen.Contains(trimmed))
						{
							seen.Add(trimmed);
							pending.Enqueue(trimmed);
							newThisFrame++;
						}
					}
					return;
				}
			}

			lock (seen)
			{
				if (seen.Count < MaxSeenCapacity && !seen.Contains(s))
				{
					seen.Add(s);
					pending.Enqueue(s);
					newThisFrame++;
				}
			}
		}

		public static void Drain(int maxPerCall = 30)
		{
			if (enqueuedThisSession >= MaxEnqueuedPerSession)
			{
				if (!limitReachedLogged)
				{
					limitReachedLogged = true;
					GATLog.Warn("UI Harvest: РґРѕСЃС‚РёРіРЅСѓС‚ Р»РёРјРёС‚ " + MaxEnqueuedPerSession + " СЃС‚СЂРѕРє Р·Р° СЃРµСЃСЃРёСЋ. РЎР±РѕСЂ РїСЂРёРѕСЃС‚Р°РЅРѕРІР»РµРЅ.");
				}
				return;
			}

			string s;
			int examined = 0;
			while (examined < maxPerCall && pending.TryDequeue(out s))
			{
				examined++;

				if (!PlaceholderGuard.ShouldTranslate(s))
				{
					filteredCount++;
					continue;
				}

				// Р¤РёР»СЊС‚СЂ РїСѓС‚РµР№, С„Р°Р№Р»РѕРІ Рё СЂР°СЃС€РёСЂРµРЅРёР№
				if (s.Contains("/") || s.Contains("\\") ||
					s.EndsWith(".png", StringComparison.OrdinalIgnoreCase) ||
					s.EndsWith(".xml", StringComparison.OrdinalIgnoreCase) ||
					s.EndsWith(".dll", StringComparison.OrdinalIgnoreCase))
				{
					filteredCount++;
					continue;
				}

				// РџСЂРѕРІРµСЂРєР° С†РёС„СЂ
				int digits = 0;
				bool hasLetter = false;
				for (int i = 0; i < s.Length; i++)
				{
					char c = s[i];
					if (char.IsDigit(c)) digits++;
					if (char.IsLetter(c)) hasLetter = true;
				}

				if (!hasLetter) { filteredCount++; continue; }
				if (digits > 0 && s.Length < 25) { filteredCount++; continue; }
				if (digits * 4 > s.Length) { filteredCount++; continue; }

				// CamelCase & Hex С„РёР»СЊС‚СЂ С‚РѕР»СЊРєРѕ РґР»СЏ РѕРґРЅРѕСЃР»РѕРІРЅС‹С… СЃС‚СЂРѕРє Р±РµР· РїСЂРѕР±РµР»РѕРІ
				if (!s.Contains(" "))
				{
					if (IsCamelCaseOrCodeIdentifier(s))
					{
						filteredCount++;
						continue;
					}
				}

				TranslateWorker.Enqueue("ui", s, isVolatile: false);
				enqueuedThisSession++;

				if (enqueuedThisSession % 300 == 0)
				{
					GATLog.Msg("UI Harvest СЃС‚Р°С‚СѓСЃ: РїРѕСЃС‚Р°РІР»РµРЅРѕ РІ РѕС‡РµСЂРµРґСЊ " + enqueuedThisSession +
					           ", РІ seen " + SeenCount + ", РѕС‚С„РёР»СЊС‚СЂРѕРІР°РЅРѕ РјСѓСЃРѕСЂР° " + filteredCount);
				}
			}
		}

		private static bool IsCamelCaseOrCodeIdentifier(string s)
		{
			if (string.IsNullOrEmpty(s)) return false;
			if (s.StartsWith("0x", StringComparison.OrdinalIgnoreCase)) return true;
			if (s.Contains("_") || s.Contains(".")) return true;

			if (s.Length >= 8)
			{
				bool allHex = true;
				for (int i = 0; i < s.Length; i++)
				{
					char c = s[i];
					if (!((c >= '0' && c <= '9') || (c >= 'a' && c <= 'f') || (c >= 'A' && c <= 'F') || c == '-'))
					{ allHex = false; break; }
				}
				if (allHex) return true;
			}

			bool hasLower = false;
			for (int i = 0; i < s.Length; i++)
			{
				char c = s[i];
				if (char.IsLower(c)) hasLower = true;
				else if (hasLower && char.IsUpper(c)) return true;
			}
			return false;
		}
	}

	/// <summary>
	/// РЎР›РћР™ 3 вЂ” РђРІС‚РѕРїРѕРґРіРѕРЅРєР° С€СЂРёС„С‚Р° РґР»РёРЅРЅС‹С… UI-РїРµСЂРµРІРѕРґРѕРІ (Widgets.Label(Rect, string)).
	/// </summary>
	[HarmonyPatch(typeof(Widgets), "Label", new[] { typeof(UnityEngine.Rect), typeof(string) })]
	public static class Patch_Widgets_Label_AutoFit
	{
		[HarmonyPrefix]
		[HarmonyPriority(Priority.Low)]
		public static void Prefix(UnityEngine.Rect rect, string label, out GameFont? __state)
		{
			__state = null;
			var s = GATMod.Settings;
			if (s == null || !s.autoFitLabels) return;

			try
			{
				if (Text.Font != GameFont.Small) return;
				if (string.IsNullOrEmpty(label) || label.Length < 4) return;
				if (rect.width < 24f || rect.height < 6f || rect.height > 200f) return;

				bool hasCyr = false;
				for (int i = 0; i < label.Length; i++)
				{
					char c = label[i];
					if (c >= '\u0400' && c <= '\u052F') { hasCyr = true; break; }
				}
				if (!hasCyr) return;

				if (Text.CalcHeight(label, rect.width) > rect.height + 0.5f)
				{
					GameFont orig = Text.Font;
					__state = orig;
					Text.Font = GameFont.Tiny;
					if (Text.Font != GameFont.Tiny)
					{
						Text.Font = orig;
						__state = null;
					}
				}
			}
			catch
			{
				if (__state.HasValue)
				{
					try { Text.Font = __state.Value; } catch { }
				}
				__state = null;
			}
		}

		[HarmonyPostfix]
		public static void Postfix(ref GameFont? __state)
		{
			if (__state.HasValue)
			{
				try
				{
					Text.Font = __state.Value;
				}
				catch { }
				finally
				{
					__state = null;
				}
			}
		}
	}

	/// <summary>
	/// Р’СЃРїРѕРјРѕРіР°С‚РµР»СЊРЅС‹Р№ РєР»Р°СЃСЃ РґР»СЏ РѕРЅР»Р°Р№РЅ-РїРµСЂРµРІРѕРґР° РґРёРЅР°РјРёС‡РµСЃРєРёС… РѕР±СЉРµРєС‚РѕРІ (Р—Р°РґР°РЅРёСЏ, РџРёСЃСЊРјР°, РЈРІРµРґРѕРјР»РµРЅРёСЏ).
	/// </summary>
	public static class DynamicTranslator
	{
		private static readonly AccessTools.FieldRef<Letter, TaggedString> letterLabelRef;
		private static readonly AccessTools.FieldRef<ChoiceLetter, TaggedString> choiceLetterTextRef;

		static DynamicTranslator()
		{
			try
			{
				letterLabelRef = AccessTools.FieldRefAccess<Letter, TaggedString>("label");
			}
			catch (Exception e)
			{
				letterLabelRef = null;
				GATLog.Warn("РќРµ СѓРґР°Р»РѕСЃСЊ РёРЅРёС†РёР°Р»РёР·РёСЂРѕРІР°С‚СЊ FieldRef РґР»СЏ Letter.label: " + e.Message);
			}

			try
			{
				choiceLetterTextRef = AccessTools.FieldRefAccess<ChoiceLetter, TaggedString>("text");
			}
			catch (Exception e)
			{
				choiceLetterTextRef = null;
				GATLog.Warn("РќРµ СѓРґР°Р»РѕСЃСЊ РёРЅРёС†РёР°Р»РёР·РёСЂРѕРІР°С‚СЊ FieldRef РґР»СЏ ChoiceLetter.text: " + e.Message);
			}
		}

		public static void TranslateQuest(Quest quest)
		{
			if (quest == null) return;

			// 1. РРјСЏ РєРІРµСЃС‚Р° (Р·Р°РіРѕР»РѕРІРѕРє)
			if (!string.IsNullOrEmpty(quest.name))
			{
				string cached;
				if (TranslationCache.TryGet("title", quest.name, out cached))
				{
					quest.name = cached;
				}
				else if (PlaceholderGuard.ShouldTranslate(quest.name))
				{
					string origName = quest.name;
					TranslateWorker.Enqueue("title", origName, v => {
						DefPostProcessor.QueueApply(() => { quest.name = v; });
					}, isVolatile: false);
				}
			}

			// 2. РћРїРёСЃР°РЅРёРµ РєРІРµСЃС‚Р°
			string descRaw = quest.description.RawText;
			if (!string.IsNullOrEmpty(descRaw))
			{
				string cached;
				if (TranslationCache.TryGet("description", descRaw, out cached))
				{
					quest.description = new TaggedString(cached);
				}
				else if (PlaceholderGuard.ShouldTranslate(descRaw))
				{
					TranslateWorker.Enqueue("description", descRaw, v => {
						DefPostProcessor.QueueApply(() => { quest.description = new TaggedString(v); });
					}, isVolatile: true);
				}
			}
		}

		public static void TranslateLetter(Letter letter)
		{
			if (letter == null) return;

			// Р—Р°РіРѕР»РѕРІРѕРє РїРёСЃСЊРјР° РЅР° РїР°РЅРµР»Рё
			try
			{
				if (letterLabelRef != null)
				{
					string labelRaw = letterLabelRef(letter).RawText;
					if (!string.IsNullOrEmpty(labelRaw))
					{
						string cached;
						if (TranslationCache.TryGet("title", labelRaw, out cached))
						{
							letterLabelRef(letter) = new TaggedString(cached);
						}
						else if (PlaceholderGuard.ShouldTranslate(labelRaw))
						{
							TranslateWorker.Enqueue("title", labelRaw, v => {
								DefPostProcessor.QueueApply(() => { letterLabelRef(letter) = new TaggedString(v); });
							}, isVolatile: false);
						}
					}
				}
			}
			catch { }

			// Р—Р°РіРѕР»РѕРІРѕРє Рё С‚РµРєСЃС‚ РґРёР°Р»РѕРіРѕРІРѕРіРѕ РѕРєРЅР° РїРёСЃСЊРјР° (ChoiceLetter)
			var cl = letter as ChoiceLetter;
			if (cl != null)
			{
				if (!string.IsNullOrEmpty(cl.title))
				{
					string cached;
					if (TranslationCache.TryGet("title", cl.title, out cached))
					{
						cl.title = cached;
					}
					else if (PlaceholderGuard.ShouldTranslate(cl.title))
					{
						string origTitle = cl.title;
						TranslateWorker.Enqueue("title", origTitle, v => {
							DefPostProcessor.QueueApply(() => { cl.title = v; });
						}, isVolatile: false);
					}
				}

				try
				{
					if (choiceLetterTextRef != null)
					{
						string textRaw = choiceLetterTextRef(cl).RawText;
						if (!string.IsNullOrEmpty(textRaw))
						{
							string cached;
							if (TranslationCache.TryGet("description", textRaw, out cached))
							{
								choiceLetterTextRef(cl) = new TaggedString(cached);
							}
							else if (PlaceholderGuard.ShouldTranslate(textRaw))
							{
								TranslateWorker.Enqueue("description", textRaw, v => {
									DefPostProcessor.QueueApply(() => { choiceLetterTextRef(cl) = new TaggedString(v); });
								}, isVolatile: true);
							}
						}
					}
				}
				catch { }

				if (cl.quest != null)
				{
					TranslateQuest(cl.quest);
				}
			}
		}
	}

	/// <summary>
	/// РЎР›РћР™ 2 вЂ” Keyed-СЃС‚СЂРѕРєРё. РџРµСЂРµС…РІР°С‚С‹РІР°РµРј С‚РѕС‚ СЃР°РјС‹Р№ РјРµС‚РѕРґ, С‡РµСЂРµР· РєРѕС‚РѕСЂС‹Р№ РёРіСЂР°
	/// РґРѕСЃС‚Р°С‘С‚ Р»СЋР±РѕР№ РїРµСЂРµРІРѕРґ РїРѕ РєР»СЋС‡Сѓ.
	/// </summary>
	[HarmonyPatch]
	public static class Patch_LoadedLanguage_TryGetTextFromKey
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ LoadedLanguage.TryGetTextFromKey вЂ” СЃР»РѕР№ Keyed РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			return AccessTools.Method(typeof(LoadedLanguage), "TryGetTextFromKey");
		}

		[HarmonyPostfix]
		public static void Postfix(string key, ref TaggedString translated, ref bool __result)
		{
			var s = GATMod.Settings;
			if (s == null || !s.translateKeyed) return;

			string current = translated.RawText;

			if (__result && !PlaceholderGuard.ShouldTranslate(current)) return;
			if (!__result || string.IsNullOrEmpty(current) || current == key) return;
			if (!PlaceholderGuard.ShouldTranslate(current)) return;

			LanguageExporter.NoteKeyed(key, current);

			string cached;
			if (TranslationCache.TryGet("keyed", current, out cached) && !string.IsNullOrWhiteSpace(cached))
			{
				// РЎС‚СЂР°С…РѕРІРєР°: РµСЃР»Рё РѕСЂРёРіРёРЅР°Р» СЃРѕРґРµСЂР¶Р°Р» ->, Р° РїРµСЂРµРІРѕРґ РїРѕС‚РµСЂСЏР»/РёР·РјРµРЅРёР» С‡РёСЃР»Рѕ СЃС‚СЂРµР»РѕРє вЂ” РІРµСЂРЅСѓС‚СЊ РѕСЂРёРіРёРЅР°Р»
				if (current.Contains("->"))
				{
					int srcArrows = CountArrows(current);
					int dstArrows = CountArrows(cached);
					if (srcArrows != dstArrows)
					{
						GATLog.Warn("Р“СЂР°РјРјР°С‚РёРєР° (->): РѕСЂРёРіРёРЅР°Р» СЃРѕРґРµСЂР¶РёС‚ " + srcArrows + " СЃС‚СЂРµР»РѕРє, РїРµСЂРµРІРѕРґ " + dstArrows +
						            ". Р’РѕР·РІСЂР°С‰Р°СЋ РѕСЂРёРіРёРЅР°Р».\n  key=" + key + "\n  orig=" + current + "\n  dst=" + cached);
						return; // РЅРµ РїРѕРґСЃС‚Р°РІР»СЏРµРј РїРµСЂРµРІРѕРґ
					}
				}

				translated = new TaggedString(cached);
				return;
			}

			TranslateWorker.Enqueue("keyed", current);
		}

		private static int CountArrows(string s)
		{
			int n = 0, at = 0;
			while ((at = s.IndexOf("->", at, StringComparison.Ordinal)) >= 0) { n++; at += 2; }
			return n;
		}
	}

	/// <summary>
	/// РЎР›РћР™ 3 V2 вЂ” РџРµСЂРµС…РІР°С‚ РѕС‚СЂРёСЃРѕРІРєРё С‚РµРєСЃС‚Р° Widgets.Label(Rect, string).
	/// </summary>
	[HarmonyPatch]
	public static class Patch_Widgets_Label_RectString
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ Widgets.Label(Rect, string) вЂ” РїРµСЂРµС…РІР°С‚ С‚РµРєСЃС‚Р° UI РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			return AccessTools.Method(typeof(Widgets), nameof(Widgets.Label), new Type[] { typeof(UnityEngine.Rect), typeof(string) });
		}

		[HarmonyPrefix]
		public static void Prefix(ref string label)
		{
			var s = GATMod.Settings;
			if (s == null || !s.translateWidgets) return;
			if (string.IsNullOrEmpty(label) || label.Length > 300) return;

			string cached;
			if (TranslationCache.TryGetFlat(label, out cached))
				label = cached;
			else
				UiHarvest.Note(label);
		}
	}

	/// <summary>
	/// РЎР›РћР™ 3 V2 вЂ” РџРµСЂРµС…РІР°С‚ РѕС‚СЂРёСЃРѕРІРєРё С‚РµРєСЃС‚Р° Widgets.Label(Rect, TaggedString).
	/// </summary>
	[HarmonyPatch]
	public static class Patch_Widgets_Label_RectTaggedString
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ Widgets.Label(Rect, TaggedString) вЂ” РїРµСЂРµС…РІР°С‚ С‚РµРєСЃС‚Р° TaggedString UI РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			return AccessTools.Method(typeof(Widgets), nameof(Widgets.Label), new Type[] { typeof(UnityEngine.Rect), typeof(TaggedString) });
		}

		[HarmonyPrefix]
		public static void Prefix(ref TaggedString label)
		{
			var s = GATMod.Settings;
			if (s == null || !s.translateWidgets) return;
			string raw = label.RawText;
			if (string.IsNullOrEmpty(raw) || raw.Length > 300) return;

			string cached;
			if (TranslationCache.TryGetFlat(raw, out cached))
				label = new TaggedString(cached);
			else
				UiHarvest.Note(raw);
		}
	}

	/// <summary>
	/// РЎР›РћР™ 3 V2 вЂ” РџРµСЂРµС…РІР°С‚ РѕС‚СЂРёСЃРѕРІРєРё С‚РµРєСЃС‚Р° Widgets.LabelFit(Rect, string).
	/// </summary>
	[HarmonyPatch]
	public static class Patch_Widgets_LabelFit
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ Widgets.LabelFit(Rect, string) вЂ” РїРµСЂРµС…РІР°С‚ РїРѕРґРіРѕРЅСЏРµРјРѕРіРѕ С‚РµРєСЃС‚Р° UI РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			return AccessTools.Method(typeof(Widgets), nameof(Widgets.LabelFit), new Type[] { typeof(UnityEngine.Rect), typeof(string) });
		}

		[HarmonyPrefix]
		public static void Prefix(ref string label)
		{
			var s = GATMod.Settings;
			if (s == null || !s.translateWidgets) return;
			if (string.IsNullOrEmpty(label) || label.Length > 300) return;

			string cached;
			if (TranslationCache.TryGetFlat(label, out cached))
				label = cached;
			else
				UiHarvest.Note(label);
		}
	}

	/// <summary>
	/// РЎР›РћР™ 3 V2 вЂ” РџРµСЂРµС…РІР°С‚ С‚РµРєСЃС‚Р° РєРЅРѕРїРѕРє Widgets.ButtonText (РїРµСЂРµРіСЂСѓР·РєР° 1).
	/// </summary>
	[HarmonyPatch]
	public static class Patch_Widgets_ButtonText
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ Widgets.ButtonText (РїРµСЂРµРіСЂСѓР·РєР° 1) вЂ” РїРµСЂРµС…РІР°С‚ С‚РµРєСЃС‚Р° РєРЅРѕРїРѕРє UI РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			return AccessTools.Method(typeof(Widgets), nameof(Widgets.ButtonText), new Type[] {
				typeof(UnityEngine.Rect), typeof(string), typeof(bool), typeof(bool), typeof(bool), typeof(Nullable<UnityEngine.TextAnchor>)
			});
		}

		[HarmonyPrefix]
		public static void Prefix(ref string label)
		{
			var s = GATMod.Settings;
			if (s == null || !s.translateWidgets) return;
			if (string.IsNullOrEmpty(label) || label.Length > 300) return;

			string cached;
			if (TranslationCache.TryGetFlat(label, out cached))
				label = cached;
			else
				UiHarvest.Note(label);
		}
	}

	/// <summary>
	/// РЎР›РћР™ 3 V2 вЂ” РџРµСЂРµС…РІР°С‚ С‚РµРєСЃС‚Р° РєРЅРѕРїРѕРє Widgets.ButtonText (РїРµСЂРµРіСЂСѓР·РєР° 2 СЃ РїР°СЂР°РјРµС‚СЂРѕРј Color).
	/// </summary>
	[HarmonyPatch]
	public static class Patch_Widgets_ButtonText_Color
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ Widgets.ButtonText (РїРµСЂРµРіСЂСѓР·РєР° 2 СЃ Color) вЂ” РїРµСЂРµС…РІР°С‚ С†РІРµС‚РЅС‹С… РєРЅРѕРїРѕРє UI РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			return AccessTools.Method(typeof(Widgets), nameof(Widgets.ButtonText), new Type[] {
				typeof(UnityEngine.Rect), typeof(string), typeof(bool), typeof(bool), typeof(UnityEngine.Color), typeof(bool), typeof(Nullable<UnityEngine.TextAnchor>)
			});
		}

		[HarmonyPrefix]
		public static void Prefix(ref string label)
		{
			var s = GATMod.Settings;
			if (s == null || !s.translateWidgets) return;
			if (string.IsNullOrEmpty(label) || label.Length > 300) return;

			string cached;
			if (TranslationCache.TryGetFlat(label, out cached))
				label = cached;
			else
				UiHarvest.Note(label);
		}
	}

	/// <summary>РџРµСЂРµС…РІР°С‚ РґРѕР±Р°РІР»РµРЅРёСЏ РЅРѕРІРѕРіРѕ Р·Р°РґР°РЅРёСЏ РІ QuestManager.</summary>
	[HarmonyPatch]
	public static class Patch_QuestManager_Add
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ QuestManager.Add вЂ” РґРёРЅР°РјРёС‡РµСЃРєРёР№ РїРµСЂРµС…РІР°С‚ РєРІРµСЃС‚РѕРІ РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			return AccessTools.Method(typeof(QuestManager), "Add");
		}

		[HarmonyPostfix]
		public static void Postfix(Quest quest)
		{
			try { DynamicTranslator.TranslateQuest(quest); } catch { }
		}
	}

	/// <summary>РџРµСЂРµС…РІР°С‚ РѕС‚РєСЂС‹С‚РёСЏ РѕРєРЅР° Р—Р°РґР°РЅРёР№ (MainTabWindow_Quests) вЂ” РїРµСЂРµРІРѕРґРёС‚ РІСЃРµ Р°РєС‚РёРІРЅС‹Рµ Р·Р°РґР°РЅРёСЏ.</summary>
	[HarmonyPatch]
	public static class Patch_MainTabWindow_Quests_PreOpen
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ MainTabWindow_Quests.PreOpen вЂ” Р°РІС‚РѕРїРµСЂРµС…РІР°С‚ СЃРїРёСЃРєР° РєРІРµСЃС‚РѕРІ РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			return AccessTools.Method(typeof(MainTabWindow_Quests), "PreOpen");
		}

		[HarmonyPostfix]
		public static void Postfix()
		{
			try
			{
				if (Find.QuestManager != null && Find.QuestManager.QuestsListForReading != null)
				{
					var list = Find.QuestManager.QuestsListForReading;
					for (int i = 0; i < list.Count; i++)
					{
						DynamicTranslator.TranslateQuest(list[i]);
					}
				}
			}
			catch { }
		}
	}

	/// <summary>РџРµСЂРµС…РІР°С‚ РїРѕР»СѓС‡РµРЅРёСЏ РїРёСЃРµРј Рё РІСЃРїР»С‹РІР°СЋС‰РёС… СЃРѕР±С‹С‚РёР№ (LetterStack.ReceiveLetter).</summary>
	[HarmonyPatch]
	public static class Patch_LetterStack_ReceiveLetter
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ LetterStack.ReceiveLetter вЂ” РїРµСЂРµРІРѕРґ РїРёСЃРµРј РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			return AccessTools.Method(typeof(LetterStack), "ReceiveLetter", new Type[] {
				typeof(Letter), typeof(string), typeof(int), typeof(bool)
			});
		}

		[HarmonyPrefix]
		public static void Prefix(Letter let)
		{
			try { DynamicTranslator.TranslateLetter(let); } catch { }
		}
	}

	/// <summary>РџСЂРёРјРµРЅСЏРµС‚ РѕС‚Р»РѕР¶РµРЅРЅС‹Рµ РёР·РјРµРЅРµРЅРёСЏ Defs, UI Рё РґРёРЅР°РјРёС‡РµСЃРєРёС… РѕР±СЉРµРєС‚РѕРІ РІ РіР»Р°РІРЅРѕРј РїРѕС‚РѕРєРµ РєР°Р¶РґС‹Р№ РєР°РґСЂ.</summary>
	[HarmonyPatch]
	public static class Patch_Root_Update
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ Verse.Root.Update вЂ” СЃР±СЂРѕСЃ Defs Рё UI РІ РіР»Р°РІРЅРѕРј РїРѕС‚РѕРєРµ РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			return AccessTools.Method(typeof(Root), "Update");
		}

		[HarmonyPostfix]
		public static void Postfix()
		{
			try { DefPostProcessor.DrainApply(); } catch { }
			try { UiHarvest.Drain(); } catch { }
		}
	}

	/// <summary>РџРµСЂРёРѕРґРёС‡РµСЃРєРёР№ СЃР±СЂРѕСЃ РєСЌС€Р° РЅР° РґРёСЃРє РІРѕ РІСЂРµРјСЏ РёРіСЂС‹.</summary>
	[HarmonyPatch]
	public static class Patch_TickManager_Autosave
	{
		private static int counter;

		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ TickManager.DoSingleTick вЂ” Р°РІС‚РѕСЃРѕС…СЂР°РЅРµРЅРёРµ РєСЌС€Р° РІС‹РєР»СЋС‡РµРЅРѕ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			return AccessTools.Method(typeof(TickManager), "DoSingleTick");
		}

		[HarmonyPostfix]
		public static void Postfix()
		{
			if (++counter < 7200) return; // ~2 РёРіСЂРѕРІС‹С… С‡Р°СЃР°
			counter = 0;
			try { TranslationCache.Flush(); } catch { }
		}
	}

	/// <summary>
	/// РЎР›РћР™ 3 V2 вЂ” РџРµСЂРµС…РІР°С‚ РІСЃРїР»С‹РІР°СЋС‰РёС… РїРѕРґСЃРєР°Р·РѕРє TooltipHandler.TipRegion(Rect, string).
	/// <summary>
	/// РџРµСЂРµС…РІР°С‚ РѕС‚РєСЂС‹С‚РёСЏ РїРёСЃРµРј (ChoiceLetter.OpenLetter).
	/// РџРµСЂРµРґ РїРѕСЃС‚СЂРѕРµРЅРёРµРј РѕРєРЅР° РґРёР°Р»РѕРіР° РїРµСЂРµС‡РёС‚С‹РІР°РµС‚ РєСЌС€ Рё С€Р°Р±Р»РѕРЅРЅС‹Р№ РёРЅРґРµРєСЃ РґР»СЏ text Рё title.
	/// </summary>
	[HarmonyPatch]
	public static class Patch_ChoiceLetter_OpenLetter
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ ChoiceLetter.OpenLetter вЂ” РѕР±РЅРѕРІР»РµРЅРёРµ РїРёСЃРµРј РїРµСЂРµРґ РѕС‚РєСЂС‹С‚РёРµРј РІС‹РєР»СЋС‡РµРЅРѕ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			return AccessTools.Method(typeof(ChoiceLetter), "OpenLetter");
		}

		[HarmonyPrefix]
		public static void Prefix(Letter __instance)
		{
			var cl = __instance as ChoiceLetter;
			if (cl == null) return;
			try
			{
				DynamicTranslator.TranslateLetter(cl);
			}
			catch { }
		}
	}

	/// <summary>
	/// РЎР›РћР™ 3 V2 вЂ” РџРµСЂРµС…РІР°С‚ РІСЃРїР»С‹РІР°СЋС‰РёС… РїРѕРґСЃРєР°Р·РѕРє TooltipHandler.TipRegion.
	/// </summary>
	[HarmonyPatch]
	public static class Patch_TooltipHandler_TipRegion
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ TooltipHandler.TipRegion(Rect, TipSignal) вЂ” РїРµСЂРµС…РІР°С‚ РїРѕРґСЃРєР°Р·РѕРє РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			foreach (var m in typeof(TooltipHandler).GetMethods(BindingFlags.Public | BindingFlags.Static))
			{
				if (m.Name != "TipRegion") continue;
				var ps = m.GetParameters();
				if (ps.Length >= 2 && ps[0].ParameterType == typeof(UnityEngine.Rect) && ps[1].ParameterType == typeof(TipSignal)) return m;
			}
			return null;
		}

		[HarmonyPrefix]
		public static void Prefix(ref TipSignal tip)
		{
			var s = GATMod.Settings;
			if (s == null || !s.translateWidgets) return;
			if (tip.textGetter != null && string.IsNullOrEmpty(tip.text)) return;
			if (string.IsNullOrEmpty(tip.text) || tip.text.Length > 600) return;

			string cached;
			if (TranslationCache.TryGetFlat(tip.text, out cached))
				tip.text = cached;
			else
				UiHarvest.Note(tip.text);
		}
	}

	/// <summary>
	/// РЎР›РћР™ 3 V2 вЂ” РџРµСЂРµС…РІР°С‚ РїРѕРґРїРёСЃРµР№ С‡РµРєР±РѕРєСЃРѕРІ Widgets.CheckboxLabeled.
	/// </summary>
	[HarmonyPatch]
	public static class Patch_Widgets_CheckboxLabeled
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ Widgets.CheckboxLabeled вЂ” РїРµСЂРµС…РІР°С‚ С‡РµРєР±РѕРєСЃРѕРІ РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			foreach (var m in typeof(Widgets).GetMethods(BindingFlags.Public | BindingFlags.Static))
			{
				if (m.Name != "CheckboxLabeled") continue;
				var ps = m.GetParameters();
				if (ps.Length >= 2 && ps[0].ParameterType == typeof(UnityEngine.Rect) && ps[1].ParameterType == typeof(string)) return m;
			}
			return null;
		}

		[HarmonyPrefix]
		public static void Prefix(ref string label)
		{
			var s = GATMod.Settings;
			if (s == null || !s.translateWidgets) return;
			if (string.IsNullOrEmpty(label) || label.Length > 600) return;

			string cached;
			if (TranslationCache.TryGetFlat(label, out cached))
				label = cached;
			else
				UiHarvest.Note(label);
		}
	}

	/// <summary>
	/// РЎР›РћР™ 3 V2 вЂ” РџРµСЂРµС…РІР°С‚ РєРѕРЅС‚РµРєСЃС‚РЅРѕРіРѕ РјРµРЅСЋ РџРљРњ (FloatMenuOption).
	/// </summary>
	[HarmonyPatch]
	public static class Patch_FloatMenuOption_Construct
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ FloatMenuOption вЂ” РїРµСЂРµС…РІР°С‚ РјРµРЅСЋ РџРљРњ РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			foreach (var ctor in typeof(FloatMenuOption).GetConstructors(BindingFlags.Public | BindingFlags.Instance))
			{
				var ps = ctor.GetParameters();
				if (ps.Length >= 1 && ps[0].ParameterType == typeof(string)) return ctor;
			}
			return null;
		}

		[HarmonyPrefix]
		public static void Prefix(ref string label)
		{
			var s = GATMod.Settings;
			if (s == null || !s.translateWidgets) return;
			if (string.IsNullOrEmpty(label) || label.Length > 600) return;

			string cached;
			if (TranslationCache.TryGetFlat(label, out cached))
				label = cached;
			else
				UiHarvest.Note(label);
		}
	}

	/// <summary>
	/// РЎР›РћР™ 3 V2 вЂ” РџРµСЂРµС…РІР°С‚ РІСЃРїР»С‹РІР°СЋС‰РёС… СѓРІРµРґРѕРјР»РµРЅРёР№ РІРІРµСЂС…Сѓ СЃР»РµРІР° (Messages.Message).
	/// </summary>
	[HarmonyPatch]
	public static class Patch_Messages_Message_String
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ Messages.Message вЂ” РїРµСЂРµС…РІР°С‚ СѓРІРµРґРѕРјР»РµРЅРёР№ РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			foreach (var m in typeof(Messages).GetMethods(BindingFlags.Public | BindingFlags.Static))
			{
				if (m.Name != "Message") continue;
				var ps = m.GetParameters();
				if (ps.Length >= 1 && ps[0].ParameterType == typeof(string)) return m;
			}
			return null;
		}

		[HarmonyPrefix]
		public static void Prefix(ref string text)
		{
			var s = GATMod.Settings;
			if (s == null || !s.translateWidgets) return;
			if (string.IsNullOrEmpty(text) || text.Length > 600) return;

			string cached;
			if (TranslationCache.TryGet("description", text, out cached))
			{
				text = cached;
			}
			else if (TranslationCache.TryGetTemplated(text, out cached))
			{
				text = cached;
			}
			else if (TranslationCache.TryGetFlat(text, out cached))
			{
				text = cached;
			}
			else
			{
				TranslateWorker.Enqueue("description", text, isVolatile: false);
			}
		}
	}


	/// <summary>
	/// РЎР›РћР™ 3 V2 вЂ” РџРµСЂРµС…РІР°С‚ С‚РµРєСЃС‚РѕРІ РґРёР°Р»РѕРіРѕРІ СЃ NPC (DiaNode TaggedString).
	/// </summary>
	[HarmonyPatch]
	public static class Patch_DiaNode_Construct_TaggedString
	{
		public static bool Prepare()
		{
			bool ok = TargetMethod() != null;
			if (!ok) GATLog.Warn("РќРµ РЅР°Р№РґРµРЅ DiaNode(TaggedString) вЂ” РїРµСЂРµС…РІР°С‚ TaggedString РґРёР°Р»РѕРіРѕРІ NPC РІС‹РєР»СЋС‡РµРЅ.");
			return ok;
		}

		public static MethodBase TargetMethod()
		{
			foreach (var ctor in typeof(DiaNode).GetConstructors(BindingFlags.Public | BindingFlags.Instance))
			{
				var ps = ctor.GetParameters();
				if (ps.Length == 1 && ps[0].ParameterType == typeof(TaggedString)) return ctor;
			}
			return null;
		}

		[HarmonyPrefix]
		public static void Prefix(ref TaggedString text)
		{
			var s = GATMod.Settings;
			if (s == null || !s.translateWidgets) return;
			string raw = text.RawText;
			if (string.IsNullOrEmpty(raw) || raw.Length > 1000) return;

			string cached;
			if (TranslationCache.TryGet("description", raw, out cached))
				text = new TaggedString(cached);
			else if (TranslationCache.TryGetTemplated(raw, out cached))
				text = new TaggedString(cached);
			else
				TranslateWorker.Enqueue("description", raw, isVolatile: false);
		}
	}
}


=== Source/TranslationCache.cs ===
using System;
using System.Collections.Concurrent;
using System.Collections.Generic;
using System.IO;
using System.Security.Cryptography;
using System.Text;
using System.Text.RegularExpressions;
using System.Linq;
using Verse;

namespace GlobalAutoTranslator
{
	/// <summary>
	/// РљСЌС€ РїРµСЂРµРІРѕРґРѕРІ. РЎРѕР·РЅР°С‚РµР»СЊРЅРѕ РќР• SQLite: System.Data.SQLite С‚СЂРµР±СѓРµС‚ РЅР°С‚РёРІРЅСѓСЋ dll,
	/// РєРѕС‚РѕСЂР°СЏ РІ Mono-СЃР±РѕСЂРєРµ RimWorld С‡Р°СЃС‚Рѕ РЅРµ РіСЂСѓР·РёС‚СЃСЏ Рё РІР°Р»РёС‚ РёРіСЂСѓ РЅР° СЃС‚Р°СЂС‚Рµ.
	/// Р’РјРµСЃС‚Рѕ СЌС‚РѕРіРѕ вЂ” С€Р°СЂРґРёСЂРѕРІР°РЅРЅС‹Рµ TSV-С„Р°Р№Р»С‹: Р±С‹СЃС‚СЂРѕ, Р±РµР· Р·Р°РІРёСЃРёРјРѕСЃС‚РµР№, Р»РµРіРєРѕ С‡РёС‚Р°РµС‚СЃСЏ РіР»Р°Р·РѕРј.
	///
	/// РџСѓС‚СЊ: %APPDATA%\..\LocalLow\Ludeon Studios\RimWorld by Ludeon Studios\GlobalTranslator\cache\
	/// </summary>
	public static class TranslationCache
	{
		private static readonly ConcurrentDictionary<string, string> map =
			new ConcurrentDictionary<string, string>(StringComparer.Ordinal);

		/// <summary>РСЃС…РѕРґРЅС‹Р№ С‚РµРєСЃС‚ РїРѕ РєР»СЋС‡Сѓ. РќСѓР¶РµРЅ РґР»СЏ СЌРєСЃРїРѕСЂС‚Р° Рё РґР»СЏ РїР»РѕСЃРєРѕРіРѕ РёРЅРґРµРєСЃР°.</summary>
		private static readonly ConcurrentDictionary<string, string> sources =
			new ConcurrentDictionary<string, string>(StringComparer.Ordinal);

		/// <summary>РџР»РѕСЃРєРёР№ РёРЅРґРµРєСЃ "РёСЃС…РѕРґРЅР°СЏ СЃС‚СЂРѕРєР° -> РїРµСЂРµРІРѕРґ" РґР»СЏ СЃР»РѕСЏ 3.
		/// Р‘РµР· MD5 Рё Р±РµР· РєРѕРЅС‚РµРєСЃС‚Р°: РІ Widgets.Label РЅРµР»СЊР·СЏ С‚СЂР°С‚РёС‚СЊ РЅРё РѕРґРЅРѕР№ Р°Р»Р»РѕРєР°С†РёРё.</summary>
		private static readonly ConcurrentDictionary<string, string> flat =
			new ConcurrentDictionary<string, string>(StringComparer.Ordinal);

		private static readonly ConcurrentDictionary<string, byte> flatNoFallback =
			new ConcurrentDictionary<string, byte>(StringComparer.Ordinal);

		public class TemplateRecord
		{
			public Regex Pattern;
			public string TargetTemplate;
			public int MinLength;
			public string Anchor;
		}

		private static readonly ConcurrentBag<TemplateRecord> templates = new ConcurrentBag<TemplateRecord>();
		private static readonly ConcurrentDictionary<string, byte> templateKeys = new ConcurrentDictionary<string, byte>(StringComparer.Ordinal);
		private static readonly ConcurrentDictionary<string, int> templateNoMatch = new ConcurrentDictionary<string, int>(StringComparer.Ordinal);
		private static int templateCount;
		private static bool templateKeysLimitLogged;
		private static bool templateCountLimitLogged;

		private static readonly ConcurrentDictionary<string, byte> dirtyShards =
			new ConcurrentDictionary<string, byte>(StringComparer.Ordinal);

		private static readonly ConcurrentDictionary<string, byte> permanentFailed =
			new ConcurrentDictionary<string, byte>(StringComparer.Ordinal);
		private static bool permanentFailedLimitLogged;
		private const int MaxPermanentFailed = 20000;

		private static readonly object ioLock = new object();

		public static int Count { get { return map.Count; } }
		public static int PermanentFailedCount { get { return permanentFailed.Count; } }

		private static string ExpectedFailedHeader
		{
			get
			{
				string modelName = GATMod.Settings != null ? GATMod.Settings.model : "default";
				return "# v" + Prompt.PromptVersion + "\t" + modelName;
			}
		}

		public static bool IsPermanentFailed(string key)
		{
			return permanentFailed.ContainsKey(key);
		}

		public static void AddPermanentFailed(string key, string source, string reason = null)
		{
			if (string.IsNullOrEmpty(key)) return;
			if (permanentFailed.Count >= MaxPermanentFailed)
			{
				if (!permanentFailedLimitLogged)
				{
					permanentFailedLimitLogged = true;
					GATLog.Warn("Р”РѕСЃС‚РёРіРЅСѓС‚ Р»РёРјРёС‚ " + MaxPermanentFailed + " Р·Р°РїРёСЃРµР№ РІ failed.tsv. Р—Р°РїРёСЃСЊ РїСЂРёРѕСЃС‚Р°РЅРѕРІР»РµРЅР°.");
				}
				return;
			}

			// Р—Р°С‰РёС‚Р° РѕС‚ РґСѓР±Р»РёРєР°С‚РѕРІ
			if (!permanentFailed.TryAdd(key, 1)) return;

			try
			{
				lock (ioLock)
				{
					string path = Path.Combine(RootDir, "failed.tsv");
					if (!File.Exists(path))
					{
						File.WriteAllText(path, ExpectedFailedHeader + "\n", Encoding.UTF8);
					}
					string entry = key + "\t" + EscapeCell(source ?? "");
					if (!string.IsNullOrEmpty(reason))
						entry += "\t" + EscapeCell(reason);
					File.AppendAllText(path, entry + "\n", Encoding.UTF8);
				}
			}
			catch (Exception e)
			{
				GATLog.Warn("РќРµ СѓРґР°Р»РѕСЃСЊ Р·Р°РїРёСЃР°С‚СЊ РІ failed.tsv: " + e.Message);
			}
		}

		public static void ClearPermanentFailed()
		{
			permanentFailed.Clear();
			permanentFailedLimitLogged = false;
			try
			{
				lock (ioLock)
				{
					string path = Path.Combine(RootDir, "failed.tsv");
					if (File.Exists(path)) File.Delete(path);
				}
				GATLog.Msg("РЎРїРёСЃРѕРє РѕРєРѕРЅС‡Р°С‚РµР»СЊРЅС‹С… РѕС‚Р±СЂР°РєРѕРІРѕРє (failed.tsv) СѓСЃРїРµС€РЅРѕ РѕС‡РёС‰РµРЅ.");
			}
			catch (Exception e)
			{
				GATLog.Warn("РќРµ СѓРґР°Р»РѕСЃСЊ СѓРґР°Р»РёС‚СЊ failed.tsv: " + e.Message);
			}
		}

		private static void LoadPermanentFailed()
		{
			try
			{
				string path = Path.Combine(RootDir, "failed.tsv");
				if (!File.Exists(path)) return;

				string[] lines = File.ReadAllLines(path, Encoding.UTF8);
				if (lines.Length == 0) return;

				// РџСЂРѕРІРµСЂРєР° Р·Р°РіРѕР»РѕРІРєР° РІРµСЂСЃРёРё РїСЂРѕРјРїС‚Р° Рё РјРѕРґРµР»Рё
				string expectedHeader = ExpectedFailedHeader;
				if (!lines[0].StartsWith("# v") || !string.Equals(lines[0].Trim(), expectedHeader, StringComparison.Ordinal))
				{
					GATLog.Warn("Р’РµСЂСЃРёСЏ РїСЂРѕРјРїС‚Р° РёР»Рё РјРѕРґРµР»Рё РёР·РјРµРЅРёР»Р°СЃСЊ (РІ С„Р°Р№Р»Рµ: " + lines[0] + ", РѕР¶РёРґР°Р»РѕСЃСЊ: " + expectedHeader + "). Р¤Р°Р№Р» failed.tsv СЃР±СЂРѕС€РµРЅ.");
					ClearPermanentFailed();
					return;
				}

				int loaded = 0;
				for (int i = 1; i < lines.Length; i++)
				{
					string line = lines[i];
					if (string.IsNullOrWhiteSpace(line)) continue;
					string[] parts = line.Split('\t');
					if (parts.Length >= 1 && !string.IsNullOrEmpty(parts[0]))
					{
						if (permanentFailed.Count < MaxPermanentFailed)
						{
							permanentFailed[parts[0]] = 1;
							loaded++;
						}
					}
				}
				if (loaded > 0)
					GATLog.Msg("Р—Р°РіСЂСѓР¶РµРЅ СЃРїРёСЃРѕРє РѕРєРѕРЅС‡Р°С‚РµР»СЊРЅС‹С… РѕС‚Р±СЂР°РєРѕРІРѕРє: " + loaded + " СЃС‚СЂРѕРє РёР· failed.tsv");
			}
			catch (Exception e)
			{
				GATLog.Warn("РћС€РёР±РєР° С‡С‚РµРЅРёСЏ failed.tsv: " + e.Message);
			}
		}

		public static string RootDir
		{
			get { return Path.Combine(GenFilePaths.SaveDataFolderPath, "GlobalTranslator"); }
		}

		public static string CacheDir { get { return Path.Combine(RootDir, "cache"); } }

		/// <summary>РљР»СЋС‡ = MD5(context|source). MD5 Р·РґРµСЃСЊ РЅРµ РєСЂРёРїС‚РѕРіСЂР°С„РёСЏ, Р° С‚РѕР»СЊРєРѕ С…РµС€ РґР»СЏ РєСЌС€Р°.</summary>
		public static string Key(string context, string source)
		{
			using (var md5 = MD5.Create())
			{
				byte[] h = md5.ComputeHash(Encoding.UTF8.GetBytes((context ?? "") + "|" + (source ?? "")));
				var sb = new StringBuilder(32);
				for (int i = 0; i < h.Length; i++) sb.Append(h[i].ToString("x2"));
				return sb.ToString();
			}
		}

		public static bool TryGet(string context, string source, out string translated)
		{
			return map.TryGetValue(Key(context, source), out translated);
		}

		public static bool TryGetByKey(string key, out string translated)
		{
			return map.TryGetValue(key, out translated);
		}

		public static bool TryGetFlat(string source, out string translated)
		{
			if (string.IsNullOrEmpty(source))
			{
				translated = null;
				return false;
			}

			if (flat.TryGetValue(source, out translated)) return true;
			if (flatNoFallback.ContainsKey(source))
			{
				translated = null;
				return false;
			}

			// 1. Р”РІРѕРµС‚РѕС‡РёРµ Рё РїСЂРѕР±РµР»С‹ РЅР° РєРѕРЅС†Рµ ("Label:" -> "РњРµС‚РєР°:")
			if (source.EndsWith(":"))
			{
				string trimmed = source.Substring(0, source.Length - 1).TrimEnd();
				if (flat.TryGetValue(trimmed, out translated))
				{
					translated = translated + source.Substring(trimmed.Length);
					flat[source] = translated;
					return true;
				}
			}

			if (source.EndsWith(": "))
			{
				string trimmed = source.Substring(0, source.Length - 2).TrimEnd();
				if (flat.TryGetValue(trimmed, out translated))
				{
					translated = translated + source.Substring(trimmed.Length);
					flat[source] = translated;
					return true;
				}
			}

			// 2. Р§РёСЃР»РѕРІРѕР№/СЋРЅРёС‚РѕРІС‹Р№ С…РІРѕСЃС‚ РїРѕСЃР»Рµ РґРІРѕРµС‚РѕС‡РёСЏ ("Global Animation Speed: 100%" -> head "Global Animation Speed", tail ": 100%")
			int colonIdx = source.LastIndexOf(':');
			if (colonIdx > 0 && colonIdx < source.Length - 1)
			{
				string tail = source.Substring(colonIdx + 1);
				bool isTailNumeric = true;
				for (int i = 0; i < tail.Length; i++)
				{
					char c = tail[i];
					if (!char.IsDigit(c) && c != ' ' && c != '%' && c != '.' && c != ',' && c != '-' && c != '+' && c != '/' && c != 'x' && c != 'Г—')
					{
						isTailNumeric = false;
						break;
					}
				}
				if (isTailNumeric)
				{
					string head = source.Substring(0, colonIdx).TrimEnd();
					if (flat.TryGetValue(head, out string headTrans))
					{
						translated = headTrans + source.Substring(head.Length);
						flat[source] = translated;
						return true;
					}
				}
			}

			// 3. Р’РѕРїСЂРѕСЃРёС‚РµР»СЊРЅС‹Р№ Р·РЅР°Рє РЅР° РєРѕРЅС†Рµ ("Enable?" -> "Р’РєР»СЋС‡РёС‚СЊ?")
			if (source.EndsWith("?"))
			{
				string trimmed = source.Substring(0, source.Length - 1).TrimEnd();
				if (flat.TryGetValue(trimmed, out translated))
				{
					translated = translated + "?";
					flat[source] = translated;
					return true;
				}
			}

			// 4. РњРЅРѕРіРѕС‚РѕС‡РёРµ РЅР° РєРѕРЅС†Рµ ("Loading..." -> "Р—Р°РіСЂСѓР·РєР°...")
			if (source.EndsWith("..."))
			{
				string trimmed = source.Substring(0, source.Length - 3).TrimEnd();
				if (flat.TryGetValue(trimmed, out translated))
				{
					translated = translated + "...";
					flat[source] = translated;
					return true;
				}
			}

			// 5. РљСЂСѓРіР»С‹Рµ СЃРєРѕР±РєРё РІРѕРєСЂСѓРі ("(Default)" -> "(РџРѕ СѓРјРѕР»С‡Р°РЅРёСЋ)")
			if (source.StartsWith("(") && source.EndsWith(")") && source.Length > 2)
			{
				string trimmed = source.Substring(1, source.Length - 2).Trim();
				if (flat.TryGetValue(trimmed, out translated))
				{
					translated = "(" + translated + ")";
					flat[source] = translated;
					return true;
				}
			}

			// 6. РљРІР°РґСЂР°С‚РЅС‹Рµ СЃРєРѕР±РєРё РІРѕРєСЂСѓРі ("[MOD]" -> "[РњРћР”]")
			if (source.StartsWith("[") && source.EndsWith("]") && source.Length > 2)
			{
				string trimmed = source.Substring(1, source.Length - 2).Trim();
				if (flat.TryGetValue(trimmed, out translated))
				{
					translated = "[" + translated + "]";
					flat[source] = translated;
					return true;
				}
			}

			flatNoFallback[source] = 1;
			translated = null;
			return false;
		}

		/// <summary>
		/// РџРѕРёСЃРє РїРѕ СЃРєРѕРјРїРёР»РёСЂРѕРІР°РЅРЅС‹Рј С€Р°Р±Р»РѕРЅР°Рј РїР»РµР№СЃС…РѕР»РґРµСЂРѕРІ РґР»СЏ СЂР°Р·СЂРµС€С‘РЅРЅС‹С… СЃС‚СЂРѕРє РїРёСЃРµРј/СѓРІРµРґРѕРјР»РµРЅРёР№.
		/// РќРРљРћР“Р”Рђ РЅРµ РІС‹Р·С‹РІР°С‚СЊ РІРѕ РІСЂРµРјСЏ OnGUI РѕС‚СЂРёСЃРѕРІРєРё.
		/// </summary>
		public static bool TryGetTemplated(string resolved, out string translated)
		{
			translated = null;
			if (string.IsNullOrEmpty(resolved) || templates.IsEmpty) return false;
			int currentTemplateCount = System.Threading.Volatile.Read(ref templateCount);
			if (templateNoMatch.TryGetValue(resolved, out int stored) && stored >= currentTemplateCount) return false;

			foreach (var t in templates)
			{
				if (resolved.Length < t.MinLength) continue;
				if (!string.IsNullOrEmpty(t.Anchor) && !resolved.Contains(t.Anchor)) continue;

				Match m;
				try
				{
					m = t.Pattern.Match(resolved);
				}
				catch (RegexMatchTimeoutException)
				{
					continue;
				}

				if (m.Success)
				{
					string res = t.TargetTemplate;
					for (int i = 1; i < m.Groups.Count; i++)
					{
						string marker = "\u0001" + (i - 1) + "\u0001";
						res = res.Replace(marker, m.Groups[i].Value);
					}
					translated = res;
					flat[resolved] = translated;
					return true;
				}
			}

			if (templateNoMatch.Count > 50000)
			{
				templateNoMatch.Clear();
				GATLog.Warn("UI Harvest: СЃР±СЂРѕС€РµРЅ РѕС‚СЂРёС†Р°С‚РµР»СЊРЅС‹Р№ РєСЌС€ С€Р°Р±Р»РѕРЅРѕРІ (> 50000).");
			}
			templateNoMatch[resolved] = System.Threading.Volatile.Read(ref templateCount);
			return false;
		}

		private static void RegisterTemplateIfAny(string source, string translated)
		{
			if (string.IsNullOrEmpty(source) || string.IsNullOrEmpty(translated)) return;
			
			if (templateKeys.Count >= 200000)
			{
				if (!templateKeysLimitLogged)
				{
					templateKeysLimitLogged = true;
					GATLog.Warn("UI Harvest: РґРѕСЃС‚РёРіРЅСѓС‚ Р»РёРјРёС‚ 200000 СѓРЅРёРєР°Р»СЊРЅС‹С… СЃС‚СЂРѕРє РґР»СЏ РїСЂРѕРІРµСЂРєРё С€Р°Р±Р»РѕРЅРѕРІ. РЎР±РѕСЂ РїСЂРёРѕСЃС‚Р°РЅРѕРІР»РµРЅ.");
				}
				return;
			}

			if (!templateKeys.TryAdd(source, 1)) return;

			MatchCollection matches = PlaceholderGuard.Ph.Matches(source);
			if (matches.Count == 0) return;

			var sbRegex = new StringBuilder("^");
			string anchor = "";
			int minLen = 0;

			var placeholderToGroupIndex = new Dictionary<string, int>(StringComparer.Ordinal);
			var groupOrder = new List<string>();
			int currentGroup = 0;
			int lastPos = 0;

			foreach (Match m in matches)
			{
				if (m.Index > lastPos)
				{
					string lit = source.Substring(lastPos, m.Index - lastPos);
					string esc = Regex.Escape(lit);
					sbRegex.Append(esc);
					minLen += lit.Length;
					if (lit.Length > anchor.Length) anchor = lit;
				}

				if (m.Value == "\\n") { sbRegex.Append(@"(?:\\n|\r?\n)"); lastPos = m.Index + m.Length; continue; }

				string phVal = m.Value;
				if (!placeholderToGroupIndex.TryGetValue(phVal, out int groupNum))
				{
					currentGroup++;
					placeholderToGroupIndex[phVal] = currentGroup;
					groupOrder.Add(phVal);
					sbRegex.Append(@"([\s\S]+?)");
				}
				else
				{
					sbRegex.Append(@"\" + groupNum);
				}

				lastPos = m.Index + m.Length;
			}

			if (lastPos < source.Length)
			{
				string lit = source.Substring(lastPos);
				string esc = Regex.Escape(lit);
				sbRegex.Append(esc);
				minLen += lit.Length;
				if (lit.Length > anchor.Length) anchor = lit;
			}

			sbRegex.Append(@"\z");

			if (anchor.Length < 12) return;

			if (System.Threading.Volatile.Read(ref templateCount) >= 5000)
			{
				if (!templateCountLimitLogged)
				{
					templateCountLimitLogged = true;
					GATLog.Warn("UI Harvest: РґРѕСЃС‚РёРіРЅСѓС‚ Р»РёРјРёС‚ 5000 С€Р°Р±Р»РѕРЅРѕРІ СЂРµРІРµСЂСЃ-РёРЅРґРµРєСЃР°. РЎР±РѕСЂ С€Р°Р±Р»РѕРЅРѕРІ РїСЂРёРѕСЃС‚Р°РЅРѕРІР»РµРЅ.");
				}
				return;
			}

			string ruPattern = translated;
			var sortedPairs = groupOrder.Select((val, idx) => new { val, idx }).OrderByDescending(x => x.val.Length).ToList();
			foreach (var pair in sortedPairs)
			{
				string marker = "\u0001" + pair.idx + "\u0001";
				ruPattern = ruPattern.Replace(pair.val, marker);
			}

			for (int i = 0; i < groupOrder.Count; i++)
			{
				if (!ruPattern.Contains("\u0001" + i + "\u0001")) return;
			}

			try
			{
				var record = new TemplateRecord
				{
					Pattern = new Regex(sbRegex.ToString(), RegexOptions.Singleline, TimeSpan.FromMilliseconds(10)),
					TargetTemplate = ruPattern,
					MinLength = minLen,
					Anchor = anchor
				};
				templates.Add(record);
				System.Threading.Interlocked.Increment(ref templateCount);
			}
			catch { }
		}

		public static void Put(string context, string source, string translated)
		{
			string key = Key(context, source);
			map[key] = translated;
			if (source != null)
			{
				sources[key] = source;
				if (context == "ui" || context == "title")
					flat[source] = translated;
				else
					flat.TryAdd(source, translated);

				byte ig;
				flatNoFallback.TryRemove(source, out ig);
				RegisterTemplateIfAny(source, translated);
			}
			dirtyShards[ShardOf(key)] = 1;
		}

		private static string ShardOf(string key)
		{
			return key.Substring(0, 2); // 256 С„Р°Р№Р»РѕРІ РјР°РєСЃРёРјСѓРј, СЂРµР°Р»СЊРЅРѕ РјРµРЅСЊС€Рµ
		}

		private static string EscapeCell(string s)
		{
			return s.Replace("\\", "\\\\").Replace("\t", "\\t").Replace("\r", "").Replace("\n", "\\n");
		}

		private static string UnescapeCell(string s)
		{
			if (s == null || s.IndexOf('\\') < 0) return s;
			var sb = new StringBuilder(s.Length);
			for (int i = 0; i < s.Length; i++)
			{
				char c = s[i];
				if (c != '\\' || i + 1 >= s.Length) { sb.Append(c); continue; }
				char n = s[i + 1];
				if (n == 'n')       { sb.Append('\n'); i++; }
				else if (n == 't')  { sb.Append('\t'); i++; }
				else if (n == '\\') { sb.Append('\\'); i++; }
				else                { sb.Append(c); }
			}
			return sb.ToString();
		}

		public static void Load()
		{
			try
			{
				Directory.CreateDirectory(CacheDir);
				LoadPermanentFailed();
				int loaded = 0, legacy = 0;
				foreach (string file in Directory.GetFiles(CacheDir, "*.tsv"))
				{
					foreach (string line in File.ReadAllLines(file, Encoding.UTF8))
					{
						if (line.Length == 0) continue;
						string[] parts = line.Split(new[] { '\t' }, 3);
						if (parts.Length == 3)
						{
							string k = parts[0];
							string srcText = UnescapeCell(parts[1]);
							string val = UnescapeCell(parts[2]);
							map[k] = val;
							if (srcText.Length > 0)
							{
								sources[k] = srcText;
								flat[srcText] = val;
								RegisterTemplateIfAny(srcText, val);
							}
							loaded++;
						}
						else if (parts.Length == 2)
						{
							// РЎС‚Р°СЂС‹Р№ С„РѕСЂРјР°С‚ Р±РµР· РєРѕР»РѕРЅРєРё РёСЃС‚РѕС‡РЅРёРєР°.
							map[parts[0]] = UnescapeCell(parts[1]);
							loaded++;
							legacy++;
						}
					}
				}
				GATLog.Msg("РљСЌС€ Р·Р°РіСЂСѓР¶РµРЅ: " + loaded + " СЃС‚СЂРѕРє РёР· " + CacheDir);
				if (legacy > 0)
					GATLog.Warn("Р—Р°РїРёСЃРµР№ СЃС‚Р°СЂРѕРіРѕ С„РѕСЂРјР°С‚Р°: " + legacy +
								". РЎР»РѕР№ 3 РґР»СЏ РЅРёС… РЅРµ СЂР°Р±РѕС‚Р°РµС‚, РїРѕРєР° РѕРЅРё РЅРµ Р±СѓРґСѓС‚ РїРµСЂРµР·Р°РїРёСЃР°РЅС‹.");
			}
			catch (Exception e)
			{
				GATLog.Warn("РќРµ СѓРґР°Р»РѕСЃСЊ Р·Р°РіСЂСѓР·РёС‚СЊ РєСЌС€: " + e);
			}
		}

		/// <summary>РЎР±СЂР°СЃС‹РІР°РµС‚ РЅР° РґРёСЃРє С‚РѕР»СЊРєРѕ РёР·РјРµРЅС‘РЅРЅС‹Рµ С€Р°СЂРґС‹.</summary>
		public static void Flush()
		{
			if (dirtyShards.Count == 0) return;
			lock (ioLock)
			{
				// РџРѕРІС‚РѕСЂРЅР°СЏ РїСЂРѕРІРµСЂРєР° РїРѕРґ Р»РѕРєРѕРј: РїРѕРєР° РјС‹ Р¶РґР°Р»Рё, РґСЂСѓРіРѕР№ РїРѕС‚РѕРє РјРѕРі РІСЃС‘ СЃР±СЂРѕСЃРёС‚СЊ.
				if (dirtyShards.Count == 0) return;

				var shards = new List<string>(dirtyShards.Keys);
				if (shards.Count == 0) return;

				// РЎРЅРёРјР°РµРј С„Р»Р°РіРё РЎР РђР—РЈ РїРѕРґ Р»РѕРєРѕРј. Р•СЃР»Рё Put() РїСЂРёР»РµС‚РёС‚ РІРѕ РІСЂРµРјСЏ Р·Р°РїРёСЃРё вЂ”
				// РѕРЅ РїРѕРјРµС‚РёС‚ С€Р°СЂРґ РіСЂСЏР·РЅС‹Рј Р·Р°РЅРѕРІРѕ, Рё РЅРѕРІС‹Рµ РґР°РЅРЅС‹Рµ СѓР№РґСѓС‚ СЃР»РµРґСѓСЋС‰РёРј Flush().
				for (int i = 0; i < shards.Count; i++)
				{
					byte ignored;
					dirtyShards.TryRemove(shards[i], out ignored);
				}

				try
				{
					Directory.CreateDirectory(CacheDir);

					var buffers = new Dictionary<string, StringBuilder>(shards.Count, StringComparer.Ordinal);
					for (int i = 0; i < shards.Count; i++)
						buffers[shards[i]] = new StringBuilder(8192);

					foreach (var kv in map)
					{
						if (kv.Key == null || kv.Key.Length < 2) continue;

						StringBuilder sb;
						if (!buffers.TryGetValue(kv.Key.Substring(0, 2), out sb)) continue;

						string srcText;
						sources.TryGetValue(kv.Key, out srcText);
						sb.Append(kv.Key).Append('\t')
						  .Append(EscapeCell(srcText ?? "")).Append('\t')
						  .Append(EscapeCell(kv.Value))
						  .Append('\n');
					}

					var encoding = new UTF8Encoding(false);
					foreach (var kv in buffers)
					{
						string path = Path.Combine(CacheDir, kv.Key + ".tsv");
						string tmp = path + ".tmp";
						File.WriteAllText(tmp, kv.Value.ToString(), encoding);
						if (File.Exists(path)) File.Delete(path);
						File.Move(tmp, path);
					}
				}
				catch (Exception e)
				{
					// Р’РѕР·РІСЂР°С‰Р°РµРј С„Р»Р°РіРё РѕР±СЂР°С‚РЅРѕ, С‡С‚РѕР±С‹ РїСЂРё РѕС€РёР±РєРµ РґРёСЃРє РґР°РЅРЅС‹Рµ РЅРµ РїРѕС‚РµСЂСЏР»РёСЃСЊ
					for (int i = 0; i < shards.Count; i++) dirtyShards[shards[i]] = 1;
					GATLog.Warn("РќРµ СѓРґР°Р»РѕСЃСЊ СЃРѕС…СЂР°РЅРёС‚СЊ РєСЌС€: " + e);
				}
			}
		}

		public static void Clear()
		{
			map.Clear();
			sources.Clear();
			flat.Clear();
			dirtyShards.Clear();
			try
			{
				if (Directory.Exists(CacheDir))
					foreach (string f in Directory.GetFiles(CacheDir, "*.tsv")) File.Delete(f);
			}
			catch (Exception e) { GATLog.Warn("РќРµ СѓРґР°Р»РѕСЃСЊ РѕС‡РёСЃС‚РёС‚СЊ РєСЌС€: " + e); }
		}

		/// <summary>РЎРЅРёРјРѕРє РІСЃРµРіРѕ РєСЌС€Р° вЂ” РЅСѓР¶РµРЅ СЌРєСЃРїРѕСЂС‚С‘СЂСѓ.</summary>
		public static Dictionary<string, string> Snapshot()
		{
			return new Dictionary<string, string>(map, StringComparer.Ordinal);
		}
	}

	public static class GATLog
	{
		private const string Tag = "[GlobalAutoTranslator] ";
		public static void Msg(string s) { Log.Message(Tag + s); }
		public static void Warn(string s) { Log.Warning(Tag + s); }
		public static void Err(string s) { Log.Error(Tag + s); }
	}
}
